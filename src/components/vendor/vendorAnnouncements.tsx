import React, { FC } from "react";

type Props = {
  name?: string;
};

export const VendorAnnouncements: FC<Props> = ({ name }) => {
  return (
    <>
      <p>Vendor Announcements</p>
    </>
  );
};
